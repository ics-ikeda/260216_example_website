# Agent Rules

## このファイルの位置づけ

- この `AGENTS.md` をコーディングルールの正本とすること。
- `.cursor/rules/index.mdc` と重複や差異がある場合は、必ず `AGENTS.md` を優先すること。

## 基本行動指針

- ユーザーとの対話は日本語で行うこと。
- 要件が曖昧な場合は、実装前に前提を短く明示してから進めること。
- ルールに書かれていない事項は、保守性・安全性・単純性を優先して判断すること。
- デザイン参照タスクでは、実装前に「見た目」「挙動」「アセット」の確認項目を短く列挙し、項目漏れを防いでから着手すること。

## 実装ポリシー

- 仕様を満たす最小実装を優先し、過剰実装を避けること。
- 重複を減らし、読みやすく変更しやすい構造を保つこと。
- 既存コードの規約・命名・構造と整合すること。
- 未使用コード・未使用ファイル・死んだ分岐は削除すること。

## 品質基準

- 変更後は利用可能な整形・静的解析・ビルド・テストを実行し、失敗を残さないこと。
- 実行できない検証がある場合は、何を未実施かを明記すること。
- エラーや警告は放置せず、原因を確認して解消すること。
- デザイン参照タスクでは、見た目一致だけでなく挙動要件とアセット運用要件まで満たした状態を完了とすること。

## Git運用

- `main` / `master` 直作業は避け、作業ブランチで進めること。
- コミットは論理的な単位で分け、メッセージは変更意図が分かる内容にすること。
- コミットメッセージは日本語で記述すること。
- ユーザーから明示指示がない限り、履歴改変や破壊的なGit操作は行わないこと。

## ドキュメント運用

- このファイルにはドメイン知識や実装技術の詳細ルールを書かないこと。
- ドメイン固有の制約が必要な場合は、別ドキュメントに分離して参照すること。
- フロントエンド実装の詳細ルールは `docs/frontend-coding-rules.md` を参照すること。
- Figma参照実装時の完了条件は `docs/frontend-coding-rules.md` の「Figma実装タスクの完了条件」を必ず満たすこと。
