# Agent Rules

## 参照ルール

- `.cursor/rules/index.mdc`

## 作業開始・検証

- 作業開始時は `npm run dev` を起動し、ブラウザ表示を確認しながら進めること。
- 修正後は必ず以下を実行し、エラー・ワーニングをゼロにすること。
  - `npm run fmt`
  - `npm run build`
- Playwrightで実画面確認を行い、DevToolsのコンソールエラー・ワーニングも解消すること。

## 基本行動指針

- ユーザーとの対話は日本語で行うこと。
- 本ドキュメントと `.cursor/rules/index.mdc` を常に遵守すること。
- 作業は `main` / `master` 直作業を避け、`feature/<task-name>` 形式のブランチで行うこと。

## HTMLルール

- `lang="ja"` を維持すること。
- `alt` と `aria` は最小限にすること。
  - 装飾画像は `alt=""`。
  - コンテンツ画像は `alt` を残す（例: Workセクション画像）。
  - 冗長・説明過多な `aria-label` は禁止。
- モーダルは `dialog` 要素を使うこと。`div role="dialog"` は使わないこと。

## TypeScriptルール

- `any` は禁止。
- 必須DOM要素は存在前提で参照し、末端までの冗長な `if`/`?.` 連鎖を持ち込まないこと。
- 意味の薄い大型ヘルパー関数は作らないこと。
- DOM属性は不要に `getAttribute()` せず、プロパティ参照を優先すること（例: `HTMLImageElement.src`）。
- コールバック型は過剰な別名型を作らず、`onFinished?: () => void;` のように直接書くこと。
- `void` 演算子は使わないこと。
- モダンブラウザ前提のため、不要な互換フォールバック分岐（例: View Transitions APIの存在判定）は書かないこと。

## CSSルール

- CSS Nestingを原則必須とし、`&` は省略可能な箇所では省略すること。
- メディアクエリは range 構文を使うこと（例: `@media (width <= 768px)`）。
- 冗長な指定を避けること。
  - 継承で済む `font-size` / `line-height` などを子要素に重複指定しない。
  - `transition` は既定値で済む指定を省略する（例: `transition: 120ms;`）。
- ベンダープレフィックスを手書きしないこと（例: `-webkit-*` 禁止）。
- `mask-image` は必要最小限。不要なマスク画像・未使用アセットは削除すること。
- 座標や幅高さなどの値は整数を使うこと（不要な小数は禁止）。
- YAGNIを徹底し、過度に複雑なカスタムプロパティ設計を避けること。
- 段階的遅延は `nth-child()` の列挙ではなく `sibling-index()` を優先すること。

## 品質保証

- 1タスク1PRを基本とし、巨大変更を避けること。
- 未使用コード・未使用アセット・死んだ分岐は必ず削除すること。
- 可読性を重視し、短すぎる変数名（例: `idx`, `cnt`, `e`, `evt`）は避けること。
